name: shared
description: >-
  API-contract types for robot-notes — DTOs, WebSocket envelopes, error codes,
  and route path constants. Pure Dart (no Flutter, no server-only deps).
  Both `server/` and `app/` depend on this package.
publish_to: none
version: 0.1.1 # x-release-please-version

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
